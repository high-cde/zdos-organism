[package]
name = "zdos-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zdos-zvm = { path = "../zvm" }
cortex = { path = "../cortex" }
